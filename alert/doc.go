// Package alert plays short synthesized notification tones for scripts and tools.
// Playback uses the system default audio device (via oto); on Linux this typically
// requires ALSA (libasound) at build time for dependents that link cgo.
package alert
